package For;

public class ForEach {
    public static void main(String[] args) {
        String[] numbers = {"1", "2", "3", "4"}; // Array "numbers"
        for (String foreach : numbers) {
            System.out.println(foreach);
        }
    }
}